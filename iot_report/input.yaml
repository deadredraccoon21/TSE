# input.yaml

# --- Database Connection Configuration ---
# Stores the credentials for connecting to the SQL Server database.
database:
  server: 'DESKTOP-JCJD4OG\SQLEXPRESS'
  name: "tse_data"
  user: "tse"
  password: "tse@123"
  driver: "ODBC Driver 11 for SQL Server"

# --- Application-Specific Configuration ---
# Defines the departments and their corresponding table names for RH and Temperature data.
app_config:
  client_name: 'Garg Acrylics (P) Ltd'
  departments:
    Spinning:
      rh_table: "Spg1_RH"
      temp_table: "Spg1_T"
    Autoconer:
      rh_table: "AutoC_RH"
      temp_table: "AutoC_T"
